I've added setup for an ai agent to assist the risk assesment.

You can see an example of what should be added in "docs/nextjs" that is a nextjs app that includes both the backend & frontend.

I want you to add the required javascript backend and frontend code into the existing security app.

Note that we are still building the app based on the spec in "docs/spec.md"

I want you to query the cyber_risk agent with the copilotkit chat widget

The agent should have the ability to filter what the user sees on the frontend - expecially - the user should be able to filter by vendor and risk severity via the chat - as the user asks to do something, the UI updates - copilotkit should power this.

Leverage the existing backend of the cyber risk app that you see in "backend" - the copilotkit functionality is simply an ai wrapper on top of that Functionality.

